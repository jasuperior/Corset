{
    "name": "corset",
    "version": "0.0.2",
    "description": "",
    "main": "index.js",
    "scripts": {
        "test": "jest",
        "build": "rm -rf dist/* && tsc -b --force",
        "build:all": "npm run build -ws --if-present && npm run build",
        "pub": "npm version patch --force && npm publish"
    },
    "workspaces": [
        "package/*"
    ],
    "keywords": [],
    "author": "Jamel Cole",
    "license": "MIT",
    "private": false,
    "devDependencies": {
        "@jest/globals": "^29.7.0",
        "ts-jest": "^29.1.2",
        "jest": "^29.7.0",
        "@types/node": "^20.11.30",
        "rimraf": "^5.0.5",
        "typescript": "^5.4.2"
    }
}
