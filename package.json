{
    "name": "corset",
    "version": "1.0.0",
    "description": "",
    "main": "index.js",
    "scripts": {
        "test": "jest",
        "build": "rm -rf dist/* && tsc -p tsconfig.pkg.json",
        "build:all": "rm -rf dist/* && tsc -p tsconfig.pkg.json -ws"
    },
    "workspaces": [
        "package/*"
    ],
    "keywords": [],
    "author": "",
    "license": "ISC",
    "private": true,
    "devDependencies": {
        "@jest/globals": "^29.7.0",
        "@types/node": "^20.11.30",
        "jest": "^29.7.0",
        "rimraf": "^5.0.5",
        "ts-jest": "^29.1.2",
        "typescript": "^5.4.2"
    }
}
